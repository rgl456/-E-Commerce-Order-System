spring:
  application:
    name: inventory-service

  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        max-attempts: 3
        initial-interval: 1000

